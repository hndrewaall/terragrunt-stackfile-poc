generate_projects:
  blocks:
    - block_name: development
      terragrunt: true
      include: "live/development/**"
      root_dir: "live/"
      workflow: default
      workflow_file: digger_workflow.yml

workflows:
  default:
    plan:
      steps:
        - plan:
