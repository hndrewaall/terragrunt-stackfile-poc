no-backend: true
generate_projects:
  blocks:
    - block_name: development
      terragrunt: true
      root_dir: "live/development/"
      workflow: default
      workflow_file: digger_workflow.yml

workflows:
  default:
    plan:
      steps:
        - init
        - plan
